{
  "name": "ai-reminder-bot",
  "version": "0.1.0",
  "bin": {
    "ai-reminder-bot": "bin/ai-reminder-bot.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "test:ci": "jest --ci --coverage --reporters=default --reporters=jest-junit --outputDirectory=test-results",
    "cdk": "cdk",
    "lambda": "cd src/functions/reminder && npx tsc && cd ../../.."
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "aws-cdk": "2.1007.0",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.186.0",
    "constructs": "^10.0.0"
  }
}
